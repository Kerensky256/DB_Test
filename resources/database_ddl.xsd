CREATE TABLE AUTHORS
        (
        AUTH_NAME VARCHAR(100) PRIMARY KEY NOT NULL
        );
        CREATE TABLE BOOKS
        (
        BOOK_NAME VARCHAR(100) PRIMARY KEY NOT NULL
        );
        CREATE TABLE BOOK_DETAILS
        (
        LIBRARY_NAME VARCHAR(100) NOT NULL,
        AUTH_NAME VARCHAR(100) NOT NULL,
        BOOK_NAME VARCHAR(100) NOT NULL,
        CAT_NAME VARCHAR(100) NOT NULL,
        PRIMARY KEY (LIBRARY_NAME, AUTH_NAME, BOOK_NAME, CAT_NAME)
        );
        CREATE TABLE CATEGORY
        (
        CAT_NAME VARCHAR(100) PRIMARY KEY NOT NULL
        );
        CREATE TABLE LIBRARY
        (
        LIBRARY_NAME VARCHAR(100) PRIMARY KEY NOT NULL
        );
        ALTER TABLE BOOK_DETAILS ADD FOREIGN KEY (AUTH_NAME) REFERENCES AUTHORS (AUTH_NAME);
        ALTER TABLE BOOK_DETAILS ADD FOREIGN KEY (BOOK_NAME) REFERENCES BOOKS (BOOK_NAME);
        ALTER TABLE BOOK_DETAILS ADD FOREIGN KEY (CAT_NAME) REFERENCES CATEGORY (CAT_NAME);
        ALTER TABLE BOOK_DETAILS ADD FOREIGN KEY (LIBRARY_NAME) REFERENCES LIBRARY (LIBRARY_NAME);
